import 'package:flutter/material.dart';

class DetailBrgPage extends StatelessWidget {
  const DetailBrgPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}